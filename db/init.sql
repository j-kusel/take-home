CREATE TABLE IF NOT EXISTS SPRUCE (ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY, name VARCHAR(255), email VARCHAR(255), address VARCHAR(255), city VARCHAR(255), state VARCHAR(2), zipcode INT, booking_type ENUM ('HOUSEKEEPING','DOG_WALK'), booking_date DATE, booking_time TIME);
